language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-octoblu-sentinel' ]; then cd .. && eval "mv $currentfolder generator-octoblu-sentinel" && cd generator-octoblu-sentinel; fi
